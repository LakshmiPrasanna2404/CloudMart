name: Deploy CloudMart

on:
  push:
    branches: [main]
    paths:
      - 'cloudformation/**'
      - 'lambdas/**'
      - 'docs/data-model/**'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ${{ vars.AWS_REGION }}
  EXEC_ROLE: ${{ vars.CFN_EXEC_ROLE_ARN }}

jobs:

  # ═══════════════════════════════════════════
  # 0. LINT
  # Python + CloudFormation
  # ═══════════════════════════════════════════
  lint:
    name: Lint Python and CloudFormation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ────────────────────────────────────────
      # Python linting
      # ────────────────────────────────────────
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install flake8
        run: pip install flake8 --quiet

      - name: Run flake8
        run: |
          flake8 lambdas/ \
            --max-line-length=120 \
            --extend-ignore=E501,W503

      # ────────────────────────────────────────
      # CloudFormation linting
      #
      # Only actual deployment templates are
      # checked here.
      # ────────────────────────────────────────
      - name: Install cfn-lint
        run: pip install cfn-lint --quiet

      - name: Lint CloudFormation templates
        run: |
          cfn-lint --ignore-checks W1011 \
            cloudformation/network-stack.yaml \
            cloudformation/data-stack.yaml \
            cloudformation/iam-stack.yaml \
            cloudformation/app-stack.yaml


  # ═══════════════════════════════════════════
  # 1. NETWORK
  # VPC, subnets, route tables, IGW, SGs
  # ═══════════════════════════════════════════
  deploy-network:
    name: Deploy network-stack
    runs-on: ubuntu-latest

    needs: lint

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy network-stack via Change Set
        run: |
          aws cloudformation deploy \
            --template-file cloudformation/network-stack.yaml \
            --stack-name cloudmart-network \
            --parameter-overrides Environment=prod \
            --role-arn ${{ env.EXEC_ROLE }} \
            --capabilities CAPABILITY_NAMED_IAM \
            --no-fail-on-empty-changeset


  # ═══════════════════════════════════════════
  # 2. DATA
  # RDS MySQL + S3
  # ═══════════════════════════════════════════
  deploy-data:
    name: Deploy data-stack
    runs-on: ubuntu-latest

    needs: deploy-network

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Fetch DB credentials from SSM
        id: dbcreds
        run: |
          USERNAME=$(aws ssm get-parameter \
            --name /cloudmart/prod/db/username \
            --with-decryption \
            --query 'Parameter.Value' \
            --output text)

          PASSWORD=$(aws ssm get-parameter \
            --name /cloudmart/prod/db/password \
            --with-decryption \
            --query 'Parameter.Value' \
            --output text)

          echo "::add-mask::$USERNAME"
          echo "::add-mask::$PASSWORD"

          echo "username=$USERNAME" >> "$GITHUB_OUTPUT"
          echo "password=$PASSWORD" >> "$GITHUB_OUTPUT"

      - name: Deploy data-stack via Change Set
        run: |
          aws cloudformation deploy \
            --template-file cloudformation/data-stack.yaml \
            --stack-name cloudmart-data \
            --parameter-overrides \
              Environment=prod \
              DBMasterUsername='${{ steps.dbcreds.outputs.username }}' \
              DBMasterUserPassword='${{ steps.dbcreds.outputs.password }}' \
            --role-arn ${{ env.EXEC_ROLE }} \
            --capabilities CAPABILITY_NAMED_IAM \
            --no-fail-on-empty-changeset


  # ═══════════════════════════════════════════
  # 3. IAM
  # Lambda execution roles and policies
  # ═══════════════════════════════════════════
  deploy-iam:
    name: Deploy iam-stack
    runs-on: ubuntu-latest

    needs: deploy-data

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy iam-stack via Change Set
        run: |
          aws cloudformation deploy \
            --template-file cloudformation/iam-stack.yaml \
            --stack-name cloudmart-iam \
            --parameter-overrides Environment=prod \
            --role-arn ${{ env.EXEC_ROLE }} \
            --capabilities CAPABILITY_NAMED_IAM \
            --no-fail-on-empty-changeset


  # ═══════════════════════════════════════════
  # 4. APPLICATION
  # Package and deploy Lambda functions
  # ═══════════════════════════════════════════
  deploy-app:
    name: Package and deploy app-stack
    runs-on: ubuntu-latest

    needs: deploy-iam

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      # ────────────────────────────────────────
      # Package Lambda functions
      # ────────────────────────────────────────
      - name: Package Lambdas with dependencies
        run: |
          for name in authorizer product apply-schema; do
            cd lambdas/$name

            pip install pymysql \
              -t . \
              --quiet \
              2>/dev/null || true

            zip -r "../../${name}.zip" . -x "*.pyc"

            cd ../..
          done

      # ────────────────────────────────────────
      # Upload Lambda packages to S3
      # ────────────────────────────────────────
      - name: Upload packages to S3
        run: |
          ARTIFACTS_BUCKET=$(aws cloudformation describe-stacks \
            --stack-name cloudmart-data \
            --query "Stacks[0].Outputs[?OutputKey=='ArtifactsBucketName'].OutputValue" \
            --output text)

          echo "ARTIFACTS_BUCKET=$ARTIFACTS_BUCKET" >> "$GITHUB_ENV"

          aws s3 cp \
            authorizer.zip \
            "s3://${ARTIFACTS_BUCKET}/lambdas/authorizer.zip"

          aws s3 cp \
            product.zip \
            "s3://${ARTIFACTS_BUCKET}/lambdas/product.zip"

          aws s3 cp \
            apply-schema.zip \
            "s3://${ARTIFACTS_BUCKET}/lambdas/apply-schema.zip"

      # ────────────────────────────────────────
      # Deploy application stack
      # ────────────────────────────────────────
      - name: Deploy app-stack via Change Set
        run: |
          aws cloudformation deploy \
            --template-file cloudformation/app-stack.yaml \
            --stack-name cloudmart-app \
            --parameter-overrides \
              Environment=prod \
              AlertEmail=${{ vars.ALERT_EMAIL }} \
            --role-arn ${{ env.EXEC_ROLE }} \
            --capabilities CAPABILITY_NAMED_IAM \
            --no-fail-on-empty-changeset

      # ────────────────────────────────────────
      # Force Lambda code updates
      # ────────────────────────────────────────
      - name: Force Lambda code updates
        run: |
          aws lambda update-function-code \
            --function-name cloudmart-prod-authorizer \
            --s3-bucket "$ARTIFACTS_BUCKET" \
            --s3-key lambdas/authorizer.zip \
            --publish

          aws lambda wait function-updated \
            --function-name cloudmart-prod-authorizer

          aws lambda update-function-code \
            --function-name cloudmart-prod-product \
            --s3-bucket "$ARTIFACTS_BUCKET" \
            --s3-key lambdas/product.zip \
            --publish

          aws lambda wait function-updated \
            --function-name cloudmart-prod-product

          aws lambda update-function-code \
            --function-name cloudmart-prod-apply-schema \
            --s3-bucket "$ARTIFACTS_BUCKET" \
            --s3-key lambdas/apply-schema.zip \
            --publish

          aws lambda wait function-updated \
            --function-name cloudmart-prod-apply-schema


  # ═══════════════════════════════════════════
  # 5. APPLY RDS SCHEMA
  # schema.sql + sample data
  # ═══════════════════════════════════════════
  apply-schema:
    name: Apply RDS schema and sample data
    runs-on: ubuntu-latest

    needs: deploy-app

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Invoke apply-schema Lambda
        run: |
          aws lambda invoke \
            --function-name cloudmart-prod-apply-schema \
            --payload '{}' \
            response.json

          cat response.json


  # ═══════════════════════════════════════════
  # 6. VERIFY AUTHENTICATION
  #
  # No token     -> 401
  # Wrong token  -> 401
  # Correct token -> 200
  # ═══════════════════════════════════════════
  verify-auth:
    name: Verify 401 / 401 / 200 auth flow
    runs-on: ubuntu-latest

    needs: apply-schema

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Get Authorizer Function URL
        id: geturl
        run: |
          URL=$(aws cloudformation describe-stacks \
            --stack-name cloudmart-app \
            --query "Stacks[0].Outputs[?OutputKey=='AuthorizerFunctionUrl'].OutputValue" \
            --output text)

          echo "url=$URL" >> "$GITHUB_OUTPUT"

      # ────────────────────────────────────────
      # Test 1 — No token
      # ────────────────────────────────────────
      - name: No token -> expect 401
        run: |
          STATUS=$(curl \
            -s \
            -o /dev/null \
            -w "%{http_code}" \
            "${{ steps.geturl.outputs.url }}products")

          echo "No-token status: $STATUS"

          [ "$STATUS" = "401" ] \
            && echo "PASS" \
            || (echo "FAIL" && exit 1)

      # ────────────────────────────────────────
      # Test 2 — Wrong token
      # ────────────────────────────────────────
      - name: Wrong token -> expect 401
        run: |
          STATUS=$(curl \
            -s \
            -o /dev/null \
            -w "%{http_code}" \
            -H "Authorization: Bearer wrong-token-value" \
            "${{ steps.geturl.outputs.url }}products")

          echo "Wrong-token status: $STATUS"

          [ "$STATUS" = "401" ] \
            && echo "PASS" \
            || (echo "FAIL" && exit 1)

      # ────────────────────────────────────────
      # Test 3 — Correct token
      # ────────────────────────────────────────
      - name: Correct token -> expect 200
        env:
          REAL_TOKEN: ${{ secrets.SMOKE_TEST_TOKEN }}
        run: |
          STATUS=$(curl \
            -s \
            -o /dev/null \
            -w "%{http_code}" \
            -H "Authorization: Bearer $REAL_TOKEN" \
            "${{ steps.geturl.outputs.url }}products")

          echo "Correct-token status: $STATUS"

          [ "$STATUS" = "200" ] \
            && echo "PASS" \
            || (echo "FAIL" && exit 1)
